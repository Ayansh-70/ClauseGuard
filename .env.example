# JuriLens Environment Configuration Example
# Copy this file to .env.local and populate with your API key.
# IMPORTANT: Never commit .env.local to version control.

# Google Gemini API Key (Server-side only)
# Used for contract clause audit and negotiation alternative generation.
GEMINI_API_KEY=
